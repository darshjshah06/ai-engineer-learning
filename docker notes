Virtualization you start with host and separate it out into a VM
host can be anything, it's hardware, controlled by stuff. 
Hypervisor is a program that is incharge of VM, it runs and manages VMs


Container has a host pc, a lightweight env that is like a box. instead of sharing apps, its sharing OS aka "Kernel"
docker can edit abd run these containers

dockerhub - hub.docker.com to get images

docker images - filename of the read only template:latest (default tag)
dockerfile - looks like:
FROM
RUN
COPY
RUN
CMD

create file - docker build -t - image - docker run - container


memory management:

# Docker deployment with memory optimization, TGI uses Flash Attention 2 and continuous batching
docker run --gpus all -p 8080:80 \
    --shm-size 1g \
    ghcr.io/huggingface/text-generation-inference:latest \
    --model-id HuggingFaceTB/SmolLM2-1.7B-Instruct \
    --max-batch-total-tokens 8192 \
    --max-input-length 4096

# Server with memory optimizations, llama.cpp uses quantization and optimized memory layout
./server \
    -m smollm2-1.7b-instruct.Q4_K_M.gguf \
    --host 0.0.0.0 \
    --port 8080 \
    -c 2048 \               # Context size
    --threads 4 \           # CPU threads
    --n-gpu-layers 32 \     # Use more GPU layers for larger models
    --mlock \               # Lock memory to prevent swapping
    --cont-batching         # Enable continuous batching


from vllm.engine.arg_utils import AsyncEngineArgs   #python, vLLM uses PagedAttention for optimal memory management

engine_args = AsyncEngineArgs(
    model="HuggingFaceTB/SmolLM2-1.7B-Instruct",
    gpu_memory_utilization=0.85,
    max_num_batched_tokens=8192,
    block_size=16,
)

llm = LLM(engine_args=engine_args)